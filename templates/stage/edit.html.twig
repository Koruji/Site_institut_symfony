{% extends 'base.html.twig' %}
{% block header %}
    <a href="{{ path('app_admin_dashboard') }}" class="nav-item btn" style="background-color: #FFDAB9;margin-right: 10px;">Menu</a>
    <a href="{{ path('app_stage_index') }}" class="nav-item btn" style="background-color: #FFDAB9;margin-right: 10px;">Retour à la liste</a>
{% endblock %}
{% block body %}
<div class="container">
    <h4 class="text-center">Modification du stage</h4>
    {{ form_start(form) }}
        {{ form_row(form.libelle) }}
        {{ form_row(form.description) }}
        {{ form_row(form.date_debut) }}
        {{ form_row(form.date_fin) }}
        {{ form_row(form.matieres) }}
        <button class="btn btn-outline-dark">{{ button_label|default('Sauvegarder') }}</button>
    {{ form_end(form) }}
</div>

{% endblock %}